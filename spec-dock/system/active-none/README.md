# active-none（placeholder）

ここは `spec-dock` が提供する **「アクティブ対象が無い」場合の置き場**です。

- `spec-dock/active/{initiative,epic,issue}` は、アクティブが未設定のレイヤーについて、
  このディレクトリ配下をリンク先として参照します。
- この配下のファイルは **編集対象外**です（best-effortで read-only）。

