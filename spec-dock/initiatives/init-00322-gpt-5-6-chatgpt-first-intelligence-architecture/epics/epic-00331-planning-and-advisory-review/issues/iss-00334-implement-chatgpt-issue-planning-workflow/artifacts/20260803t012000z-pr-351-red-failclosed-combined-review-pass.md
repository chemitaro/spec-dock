# Formal Read-Only Red Team Review

## 1. Verified review identity

| Field                    | Verified value                                                                                                         |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------- |
| Repository               | `chemitaro/spec-dock`                                                                                                  |
| Required branch          | `iss-00334-implement-chatgpt-issue-planning-workflow`                                                                  |
| Pull request             | `#351`, open                                                                                                           |
| Required source HEAD     | `1fba52b2357931a20b5ef68bf6e40bb53dbd243a`                                                                             |
| Branch HEAD              | Exact match; GitHub comparison returned `identical`, ahead `0`, behind `0`                                             |
| PR head branch           | Exact required branch                                                                                                  |
| PR head SHA              | Exact required source HEAD                                                                                             |
| Default-branch fallback  | Not used                                                                                                               |
| Review role              | Fresh Red Team, read-only                                                                                              |
| Review model             | GPT-5.6 Pro                                                                                                            |
| Model-selection evidence | Current-session runtime model identity. No separate Oracle selector transcript was generated by this read-only review. |

GitHub reports PR #351 with the required feature branch as its head and `1fba52b2357931a20b5ef68bf6e40bb53dbd243a` as the head SHA.  The review scope and exact-source requirements were taken from the attached Red Team contract. 

No finding was attributed to an older Candidate or earlier source snapshot. The authoritative review target was the exact GitHub source HEAD above; attached transcripts and report entries were treated only as supplementary evidence.

## 2. Inspected files and tests

| Surface                                                                                              | Inspection result                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Provider `src/spec_dock/assets/spec_dock/scripts/spec_dock_runtime/infra/issue_planning_review.py`   | Inspected at the exact source HEAD. Blob SHA: `5a92bf86679ff44bdf4adf125dc4fb717ed95a27`. `_remove_evidence_directory_at()` performs no deletion and unconditionally returns `False`. No executable `os.unlink()` or `os.rmdir()` call is present in that cleanup path or a replacement cleanup helper.                                                                                                             |
| Dogfood `spec-dock/scripts/spec_dock_runtime/infra/issue_planning_review.py`                         | Inspected at the exact source HEAD. Blob SHA is also `5a92bf86679ff44bdf4adf125dc4fb717ed95a27`; provider/dogfood byte parity is established.                                                                                                                                                                                                                                                                       |
| Application `src/spec_dock/assets/spec_dock/scripts/spec_dock_runtime/application/issue_planning.py` | `PlanningPublicationSourceStale` alone maps to `stale/review_target_changed`. `OSError`, `UnicodeError`, or `ValueError` maps to `blocked/review_publication_failed`. `ok/review_completed` is returned only after publication returns successfully.                                                                                                                                                                |
| `tests/unit/infra/test_issue_planning_review.py`                                                     | Inspected successful publication, no-replace collision, staging/output identity substitution, and fail-closed cleanup coverage. The cleanup regression is parameterized across both guard modes—`false` and `exception`—and both prohibited destructive operations—`unlink` and `rmdir`. It fails immediately if either operation is invoked and verifies preservation of the sentinel, review result, and summary. |
| Prior formal Red evidence                                                                            | Confirmed the earlier defect was the check-then-delete `stat`→`unlink`/`rmdir` race at HEAD `7ed51a133ab551ed22fd41c5856315256fcaace2`.                                                                                                                                                                                                                                                                             |
| Blue repair boundary                                                                                 | Confirmed the accepted fail-closed rule: where identity-conditional removal cannot be proven, preserve evidence, return publication failure, and do not produce `ReviewSourceStale` or `review_completed`.                                                                                                                                                                                                          |
| Current report evidence                                                                              | The report records focused infra `15 passed`, ordinary pytest `1384 passed, 2235 skipped`, lint/type checks, SpecDock validation, diff check, and provider/dogfood parity. These execution claims were inspected as formal evidence but were not independently rerun in this connector-only review.                                                                                                                 |

### Behavioral verification

**Guard true:** staging contents and identities are verified, publication uses atomic no-replace rename, the parent directory is synchronized, and a successful guard permits `ok/review_completed`.

**Guard false:** publication has already completed, cleanup returns `False` without invoking a destructive pathname operation, the published result and summary remain intact, and the resulting `OSError` is normalized by the application to `blocked/review_publication_failed`. It cannot become `ReviewSourceStale` or `review_completed`.

**Guard exception:** the guard exception is wrapped by a fixed publication error; the non-destructive cleanup remains unavailable, the evidence stays intact, and the application returns the same content-free `blocked/review_publication_failed` result. The cause, path, URL, and private directory name are not copied into result output or details.

**Collision and output identity:** existing no-replace collision behavior and output-directory identity checks remain present. The cleanup repair does not bypass or weaken them.

## 3. P0/P1 findings

**None.**

The prior `REVIEW-QUARANTINE-DESTRUCTIVE-TOCTOU` defect is closed at this exact HEAD. The former destructive authorization gap no longer exists because the cleanup path performs no `unlink` or `rmdir` operation at all. Ambiguous cleanup is not promoted to proven stale, and preserving the published evidence is the explicitly accepted behavior rather than an availability or contract defect.

No concrete P0/P1 safety, correctness, contract, or availability regression was identified within the bounded Issue scope.

## 4. Counts and verdict

| Severity | Count |
| -------- | ----: |
| P0       |     0 |
| P1       |     0 |

**Overall verdict: PASS**

## 5. Read-only statement

No patch, replacement ZIP, generated revision, repository mutation, branch update, PR update, or artifact modification was produced.
