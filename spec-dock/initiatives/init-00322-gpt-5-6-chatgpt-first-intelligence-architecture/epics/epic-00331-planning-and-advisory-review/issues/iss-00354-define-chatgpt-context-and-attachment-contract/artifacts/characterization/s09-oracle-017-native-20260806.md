# S09 Oracle 0.17.0 native characterization receipt

## Scope

これは `iss-00354` の S09 で、PATH Oracle 0.17.0 のブラウザ実行と成果物転送の観測だけを記録する。仕様採用、実装完了、レビュー判定、S09 closure の証跡ではない。

## Sanitized observation

| 項目 | 観測結果 |
|---|---|
| Oracle version | `0.17.0` |
| engine | browser |
| requested model | `gpt-5.6` |
| observed model | `GPT-5.6 Sol` |
| model strategy | `select` |
| model verified | `true` |
| prompt submitted | `true` |
| response | `response-complete=true` |
| artifact link | `oracle-017-characterization.zip` の sandbox link を応答内で観測 |
| expected logical filename | `oracle-017-characterization.zip` |
| expected internal root | `oracle-017-characterization` |
| ZIP transfer | 未取得。Oracle の download-button fallback は期待ファイルを保存しなかった |
| same-session harvest | `oracle session <id> --harvest --no-recover` を一回実行。応答本文は再取得できたが ZIP は保存されなかった |
| session artifact inventory | transcript のみ。ZIP artifact metadata / SHA / size は未観測 |
| characterization status | `blocked`（0.17.0 artifact transfer/schema、capture builder、reader bindingが未確定） |

## Capability help surface

0.17.0 の `oracle --help` で次の root option token を確認した。

`--engine`, `--file`, `--slug`, `--wait`, `--prompt`, `--browser-attachments`, `--model`, `--browser-model-strategy`, `--remote-chrome`, `--browser-no-cookie-sync`

`oracle session --help` で次の session option token を確認した。

`--harvest`, `--no-recover`, `--write-output`, `--browser-tab`, `--live`

## Boundary

- 0.17.0 の exact harvest/capture argv、prompt/response/model metadata field、inline capability、成果物 metadata schema、0.17 reader の安全な共有範囲は未確定のまま保持する。
- 0.16.1 の profile extraction はこの receipt によりブロックされないが、0.17.0 profile/readerを推測登録してはならない。
- 個人設定、認証情報、絶対パス、target URL、session handle、raw transcript、ZIP実体はこの証跡に含めない。

## Evidence source

- PATH Oracle native browser run: `iss354-s09-native`
- same-session harvest: one attempt, no recovery relaunch
- model evidence: Oracle browser model-selection receipt (`GPT-5.6 Sol`, `select`, `verified=true`)
