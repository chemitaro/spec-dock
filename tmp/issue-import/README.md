# 一時ドキュメント（削除予定）

このディレクトリは、機能追加（既存 Issue / 既存ブランチの import）に向けた **要件→設計→実装計画**の議論用メモ置き場です（後で削除する前提）。

配置しているファイルは、spec-dock の Issue テンプレートをそのままコピーしたものです:

- `requirement.md`（要件定義）
- `design.md`（設計）
- `plan.md`（実装計画）

必要に応じて `<ISS_ID>` / `<ISS_TITLE>` などのプレースホルダを置換し、議論ログとして更新してください。

