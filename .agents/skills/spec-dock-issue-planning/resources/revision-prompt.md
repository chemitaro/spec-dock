# Issue Planning Semantic Revision

Produce a complete replacement authoring ZIP for `requirement.md`, `design.md`, `plan.md`, and
exactly one subordinate onboarding companion from the
supplied prior Candidate, exact formal Review evidence, selected P0/P1 findings, and preserved
assumptions. Keep the supplied Issue, parent, repository, branch, and source HEAD identity unchanged.

Return exactly one downloadable ZIP matching the stated logical filename, root, and closed inventory.
The companion has the same authority, content, and four PlantUML diagram-role obligations as Planner.
Do not return a patch, partial document set, marker frame, Candidate control file,
Human decision, approval claim, session locator, conversation identifier, or repository mutation.
