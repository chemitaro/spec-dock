# Issue Planning Planner

Create a complete advisory Issue Planning authoring package for the exact existing Issue.
Produce exactly one downloadable ZIP whose logical filename, internal root, and closed inventory
match the role-specific expectation in this Prompt. Include complete `requirement.md`, `design.md`,
`plan.md`, and exactly one subordinate new-member onboarding companion at the stated path.

The companion must declare canonical precedence and cover lineage, purpose, scope, current and
target architecture, ChatGPT First workflow, provider-owned direct Oracle versus reference-only
operator `chatgpt-use`, Candidate/Review/Human/apply lifecycle, exact-branch failure, current status,
remaining roadmap, provider/projection, failure modes, and a first-day checklist. It must contain
at least four valid `plantuml` fenced blocks with distinct system-context, responsibility-boundary,
planning-sequence, and implementation-roadmap roles.

Do not return inline document concatenation, a marker frame, patch, diff, Candidate control files,
extra prose, or any approval/adoption/implementation-readiness claim. Do not mutate the repository,
commit, push, merge, finish the Issue, or replace Human authority.
