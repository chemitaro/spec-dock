name = "spec-dock"
description = "Native shim for spec-dock orchestration. Delegate all work to `.agents/skills/spec-dock-codex-adapter/SKILL.md`."

developer_instructions = """
Use this native shim only as a Codex discovery/delegation entrypoint for spec-dock.
Delegate spec-dock workflow execution to `.agents/skills/spec-dock-codex-adapter/SKILL.md`.
Do not reimplement spec-dock protocol or generated state behavior in this shim.
"""
