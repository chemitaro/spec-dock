# Root Artifact Rules

Files imported with `artifact import file --root` are opaque, evidence-only
Artifacts. Their presence does not make their contents canonical, reviewed,
approved, or adopted.

When root Artifact storage is initialized, `spec-dock/artifacts/rules.md` is a
relative symlink to this provider-managed rules source.
